package com.airsaid.ktx

/**
 * Pre-allocated empty byte array to avoid repeated allocations.
 * Use this constant when an empty byte array is needed.
 */
val emptyByteArray = ByteArray(0)

/**
 * Pre-allocated empty int array to avoid repeated allocations.
 * Use this constant when an empty int array is needed.
 */
val emptyIntArray = IntArray(0)

/**
 * Pre-allocated empty float array to avoid repeated allocations.
 * Use this constant when an empty float array is needed.
 */
val emptyFloatArray = FloatArray(0)

/**
 * Pre-allocated empty long array to avoid repeated allocations.
 * Use this constant when an empty long array is needed.
 */
val emptyLongArray = LongArray(0)

/**
 * Pre-allocated empty boolean array to avoid repeated allocations.
 * Use this constant when an empty boolean array is needed.
 */
val emptyBooleanArray = BooleanArray(0)

/**
 * Pre-allocated empty string array to avoid repeated allocations.
 * Use this constant when an empty string array is needed.
 */
val emptyStringArray = Array(0) { "" }


